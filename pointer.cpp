#include <iostream>
using namespace std;
int main(){
    
    //POINTER  CONCEPT

    // int var = 10;
    // char str[10];
    // int *ptr = &var;
    // cout<<"value of var";
    // cout<<*ptr;
    // cout<<"\n";
    // cout<<"address of var";
    // cout<<ptr;
    // cout<<"\n";
    // cout<<"address of str";
    // cout<<&str;

    //POINTER Arithmetic

    // int MAX = 3;
    // int var[MAX] = {10,20,30};
    // int *ptr;
    // ptr=var;//pointing at the first element of the array var[MAX]
    // for(int i = 0; i < MAX; i++){
    //     cout<<"The address of index"<<i<<" is "<<ptr<<"\n";
    //     cout<<"The value of index"<<i<<" is "<<*ptr<<"\n";
    //     ptr++;
    // }

//pointers of arrays

//    int MAX = 3;
//    int var[MAX] = {1,2,3};
//    int *ptr[MAX];

//    for(int i = 0;i<MAX;i++){
//     ptr[i] = &var[i];
//    }

//    for (int i = 0; i < MAX; i++){
//     cout<<"The value of "<<i<<" is "<<ptr[i]<<"\n";
//    }

// int MAX = 4;
// char *names[MAX] = { "Haider Ali", "Iftekar Ali", "Sahil Ali", "Zubina Ali" };

//    for (int i = 0; i < MAX; i++) {
//       cout << "name of terrorist [" << i << "] = ";
//       cout << *(names+i) << endl;
//       cout<<"location coordinates : "<<(names+i)<<endl;
      

//    }
   

    return 0;
}